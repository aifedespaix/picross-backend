# Migration `watch-20191028081439`

This migration has been generated by aife at 10/28/2019, 8:14:39 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_User" (
  "email" TEXT NOT NULL DEFAULT ''  ,
  "id" TEXT NOT NULL   ,
  "level" TEXT NOT NULL DEFAULT 'ADMIN'  ,
  "name" TEXT    ,
  "password" TEXT NOT NULL DEFAULT ''  ,
  PRIMARY KEY ("id")
);

INSERT INTO "new_User" ("email","id","name","password") SELECT "email","id","name","password" from "User"

DROP TABLE "lift"."User";

ALTER TABLE "lift"."new_User" RENAME TO "User";

CREATE UNIQUE INDEX "lift"."User.email" ON "User"("email")

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;

PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_Picross" (
  "author" TEXT    REFERENCES "User"(id) ON DELETE SET NULL,
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "id" TEXT NOT NULL   ,
  "likes" INTEGER NOT NULL DEFAULT 0  ,
  "map" TEXT NOT NULL DEFAULT ''  ,
  "title" TEXT NOT NULL DEFAULT ''  ,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "validated" BOOLEAN NOT NULL DEFAULT false  ,
  PRIMARY KEY ("id")
);

INSERT INTO "new_Picross" ("createdAt","id","map","updatedAt") SELECT "createdAt","id","map","updatedAt" from "Picross"

DROP TABLE "lift"."Picross";

ALTER TABLE "lift"."new_Picross" RENAME TO "Picross";

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration watch-20191015081126..watch-20191028081439
--- datamodel.dml
+++ datamodel.dml
@@ -6,8 +6,13 @@
   provider = "sqlite"
   url      = "file:dev.db"
 }
+enum Level {
+  ADMIN
+  USER
+}
+
 model Post {
   id        String   @default(cuid()) @id
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
@@ -17,17 +22,23 @@
   author    User?
 }
 model User {
-  id       String  @default(cuid()) @id
-  email    String  @unique
-  password String
-  name     String?
-  posts    Post[]
+  id        String  @default(cuid()) @id
+  email     String  @unique
+  password  String
+  name      String?
+  posts     Post[]
+  picrosses Picross[]
+  level     Level
 }
 model Picross {
   id        String  @default(cuid()) @id
   map       String
+  author    User
+  title     String
+  likes     Int      @default(0)
+  validated Boolean  @default(false)
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
 }
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20191028081439)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20191028081439'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
